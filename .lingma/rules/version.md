---
trigger: always_on
---
- 这是一个针对linux系统平台的Rust项目，用于可视化管理ftp/sftp服务器的配置和服务。

## 项目规则
- 所有文件都必须使用UTF-8编码
- 项目使用 edition = "2024" 版本，确保代码符合最新的Rust语言规范。
- 能异步实现的都必须实现为异步函数
- 每次修改代码前，必须先阅读cargo.toml文件，学习组件版本文档和api使用方法
- 新增组件使用cargo add 组件名称 添加到cargo.toml文件中以避免引入错误版本的组件
- 每次修改bug后，必须执行cargo clippy检查并修复问题，不允许隐藏任何告警信息
- rust 版本0.xx.yy 每次修改代码更新yy一次
- 测试用ftp/sftp 用户 用户名：123 密码：123456

## 测试规则
- 测试过程中直接在后台运行wftpd程序